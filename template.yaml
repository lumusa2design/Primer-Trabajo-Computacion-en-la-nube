AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31

Description: CRUD API para Computacion en la Nube

Resources:

  ItemsSamTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: ItemsSam
      BillingMode: PAY_PER_REQUEST

      AttributeDefinitions:
        - AttributeName: id
          AttributeType: S

      KeySchema:
        - AttributeName: id
          KeyType: HASH

  CrudFunction:
    Type: AWS::Serverless::Function
    Properties:

      CodeUri: src/

      Handler: app.lambda_handler

      Runtime: python3.13

      Timeout: 10

      Environment:
        Variables:
          TABLE_NAME: ItemsSam

      Role: arn:aws:iam::223977122891:role/LabRole

      Events:

        CreateItem:
          Type: Api
          Properties:
            Path: /ItemsSam
            Method: post

        GetItemsSam:
          Type: Api
          Properties:
            Path: /ItemsSam
            Method: get

        GetItem:
          Type: Api
          Properties:
            Path: /ItemsSam/{id}
            Method: get

        UpdateItem:
          Type: Api
          Properties:
            Path: /ItemsSam/{id}
            Method: put

        DeleteItem:
          Type: Api
          Properties:
            Path: /ItemsSam/{id}
            Method: delete

Outputs:

  ApiUrl:
    Description: URL API Gateway
    Value: !Sub "https://${ServerlessRestApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/"
